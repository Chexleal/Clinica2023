@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>Inicio - Traumha</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/pages/login.css" asp-append-version="true" />
</head>
<body>
    <div class="container vh-100 d-flex justify-content-center align-items-center">
        <div class="mb-md-1 mt-md-1">
            <div class="mx-auto my-auto">
                <form id="loginForm" onclick="alertPrevent(event)" action="@Url.Action("LogIn")" method="post" class="form text-center">
                    <div class="logo">
                        <img src="~/img/Logo.png" alt="Logo" style="width:80%" class="mb-3">
                    </div>
                    <input type="text" id="user" class="mb-3" placeholder="Usuario" name="user" />
                    <input type="password" id="password" class="mb-3" placeholder="Contraseña" name="password" />
                    <a class="forgot mb-4" href="@Url.Action("RecuperarCuenta")">¿Olvidó su contraseña?</a>
                    <button type="submit">Entrar</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>

@section Scripts{
    
    <script>

        //const form = document.getElementById('loginForm');

        //form.addEventListener('submit', function (event) {
        //    const usernameInput = document.querySelector('input[name="user"]');
        //    if (!usernameInput.value) {
        //        event.preventDefault(); // Prevent form submission
        //        alert('Please enter your username');
        //    }
        //});

        function alertPrevent(e) {
            e.preventDefault();

            Swal.fire({
                title: '¿Esta seguro de eliminar este servicio?',
                icon: 'question',
                showCancelButton: true,
                confirmButtonText: 'Si',
                cancelButtonText: 'No'
            }).then((resultado) => {

                if (resultado.isConfirmed) {
                    const formulario = document.getElementById('loginForm');
                    formulario.submit();
                }

            })
        }

        function testAlert() {
            Swal.fire('Any fool can use a computer');
        }
    </script>

}