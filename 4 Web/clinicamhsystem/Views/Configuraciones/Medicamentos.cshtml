@model List<ClinicaDomain.Medicamento>
@{
    ViewData["Title"] = "Catálogo Medicamentos";
}
<div class="card shadow-lg p-3 mb-4">
    <div class="card-header bg-white d-flex align-items-center gap-2">
        <a href="@Url.Action("Index","Configuraciones")" class="btn btn-outline-secondary btn-sm" title="Volver a Configuraciones"><i class="fa-solid fa-arrow-left"></i></a>
        <h4 class="mb-0"><i class="fa-solid fa-pills" style="color:#2d8f77"></i> Catálogo Medicamentos</h4>
    </div>
    <div class="card-body">
        <form method="post" action="@Url.Action("CrearMedicamento")" class="row g-2 align-items-end mb-3">
            <div class="col-12 col-md-10">
                <label class="fw-bold small">Nombre</label>
                <input name="nombre" class="form-control form-control-sm" style="text-transform:uppercase;" placeholder="Ej: Ibuprofeno 400mg" maxlength="300" required />
            </div>
            <div class="col-12 col-md-2">
                <button type="submit" class="btn btn-dark btn-sm w-100"><i class="fa-solid fa-plus"></i> Agregar</button>
            </div>
        </form>
        <hr class="mt-1 mb-3" />
        <div class="table-responsive">
            <table class="table table-sm table-hover tabla-config" data-search-placeholder="Buscar medicamento" data-export-name="Medicamentos">
                <thead class="table-dark"><tr><th>Nombre</th><th></th></tr></thead>
                <tbody>
                @foreach(var m in Model.OrderBy(x => x.Nombre)){
                    <tr>
                        <td>@m.Nombre</td>
                        <td class="text-end text-nowrap">
                            <partial name="_AuditInfo" model="m" />
                            <form method="post" action="@Url.Action("EliminarMedicamento")" style="display:inline" onsubmit="return confirm('¿Eliminar?')">
                                <input type="hidden" name="id" value="@m.IdMedicamento" />
                                <button type="submit" class="btn btn-sm btn-outline-danger"><i class="fa-solid fa-trash"></i></button>
                            </form>
                        </td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts{
    <partial name="_DataTablesButtonsScripts" />
    <script src="~/js/pages/audit-info.js" asp-append-version="true"></script>
    <script src="~/js/pages/tablas-config.js" asp-append-version="true"></script>
}
